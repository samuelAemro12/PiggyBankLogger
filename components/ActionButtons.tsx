import React from 'react';
import { Button, View } from 'react-native';

const ActionButtons = () => (
  <View>
    <Button title="+ Add Deposit" onPress={() => {}} />
    <Button title="- Add Expense" onPress={() => {}} />
  </View>
);

export default ActionButtons;
